{
    "name": "pecan-client-spec",
    "version": "1.0.0",
    "description": "pecan api spec for vue client",
    "main": "index.ts",
    "scripts": {
        "build": "rm -rf dist && npx tsc",
        "prettier:check": "npx prettier . --check",
        "prettier:format": "npx prettier . --write",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [],
    "author": "Wayne W",
    "license": "ISC",
    "devDependencies": {
        "@types/node": "^24.3.0",
        "prettier": "3.6.2",
        "typescript": "^5.9.2"
    }
}
